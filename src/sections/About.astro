---
import { personalInfo } from '../data/portfolio'
---

<section
  id="about"
  class="min-h-screen bg-linear-to-br from-blue-950 via-slate-950 to-blue-950 py-20 px-4 relative flex items-center justify-center"
>
  <!-- Pixel grid background -->
  <div class="absolute inset-0 pixel-grid opacity-20"></div>

  <!-- Gaming Retro Container -->
  <div class="max-w-5xl w-full relative">
    <!-- Shadow layer -->
    <div class="absolute inset-0 border-4 border-cyan-400 translate-x-2 translate-y-2 rounded-lg"></div>
    <!-- Main container -->
    <div class="bg-blue-900/40 backdrop-blur-sm border-4 border-cyan-400 rounded-lg p-6 md:p-12 lg:p-20 relative">
      <!-- Tabs - Gaming Style -->
      <div class="hidden md:flex absolute right-0 top-32 flex-col gap-2 z-20">
        <div
          class="bg-blue-900 border-2 md:border-3 border-cyan-400 px-2 md:px-3 py-6 md:py-8 tab mono-font text-[10px] md:text-xs font-bold text-cyan-400 shadow-md hover:shadow-lg hover:bg-blue-800 transition"
        >
          WORK
        </div>
        <div
          class="bg-blue-800 border-2 md:border-3 border-cyan-400 px-2 md:px-3 py-6 md:py-8 tab mono-font text-[10px] md:text-xs font-bold text-cyan-400 shadow-md hover:shadow-lg hover:bg-blue-700 transition"
        >
          ABOUT
        </div>
        <div
          class="bg-cyan-400 border-2 md:border-3 border-cyan-300 px-2 md:px-3 py-6 md:py-8 tab mono-font text-[10px] md:text-xs font-bold text-blue-900 shadow-md hover:shadow-lg"
        >
          RESUME
        </div>
        <div
          class="bg-blue-500 border-2 md:border-3 border-cyan-400 px-2 md:px-3 py-6 md:py-8 tab mono-font text-[10px] md:text-xs font-bold text-white shadow-lg pulse-glow"
        >
          SKETCHBOOK
        </div>
      </div>

      <div class="relative">
        <!-- Pixel Icon -->
        <div
          class="w-20 h-20 md:w-24 md:h-24 bg-cyan-400 flex items-center justify-center text-3xl md:text-4xl mb-4 md:mb-6 floating relative pixel-border"
          style="border-radius: 0; box-shadow: 0 4px 12px rgba(34, 211, 238, 0.4), 4px 4px 0 var(--color-dark-blue);"
        >
          <div class="absolute inset-0 pixel-grid opacity-20"></div>
          <span class="relative z-10">✏️</span>
        </div>

        <!-- Level Badge -->
        <div class="inline-block mb-6 md:mb-8">
          <div class="bg-blue-900/60 border-2 border-cyan-400 px-4 py-2 relative">
            <div class="absolute -top-1 -left-1 w-2 h-2 bg-cyan-400"></div>
            <div class="absolute -top-1 -right-1 w-2 h-2 bg-cyan-400"></div>
            <div class="absolute -bottom-1 -left-1 w-2 h-2 bg-cyan-400"></div>
            <div class="absolute -bottom-1 -right-1 w-2 h-2 bg-cyan-400"></div>
            <span class="pixel-font text-[10px] md:text-xs text-cyan-400">⚡ LEVEL 1 - ABOUT ME</span>
          </div>
        </div>

        <!-- Title - Pixel Font -->
        <h2
          class="text-3xl md:text-5xl lg:text-6xl font-black text-cyan-400 mb-2 pixel-font leading-tight tracking-wider"
        >
          {personalInfo.name}<br />{personalInfo.lastName.split(' ')[0]}
        </h2>
        <div class="w-24 md:w-32 h-1 bg-linear-to-r from-cyan-400 to-blue-400 mb-6 md:mb-8 shadow-sm shadow-cyan-400/50">
        </div>

        <!-- Subtitle -->
        <p
          class="mono-font text-cyan-300 text-base md:text-lg mb-4 md:mb-6 flex items-center gap-2 font-bold"
        >
          <span class="w-2 h-2 md:w-3 md:h-3 bg-cyan-400 inline-block"></span>
          <span>{'>'}</span>
          {personalInfo.role.split('|')[0].trim()} | {personalInfo.role.split('|')[1].trim()}
        </p>

        <!-- Description with pixel arrow -->
        <div class="relative mb-6 md:mb-8">
          <p
            class="text-cyan-200 text-lg md:text-xl leading-relaxed max-w-2xl font-medium mono-font"
          >
            {'>'} Ingeniero en Desarrollo y Gestión de Software con experiencia en tecnologías web y móvil.
          </p>
          <!-- Pixel-style arrow -->
          <div class="hidden md:block absolute -right-8 -bottom-4 text-4xl text-cyan-400">→</div>
          <p class="mono-font text-cyan-400 mt-3 md:mt-4 text-sm md:text-base">
            {'>'} Desarrollando soluciones óptimas para el cliente AUDI México.
          </p>
        </div>

        <!-- Buttons - Pixel Gaming Style -->
        <div class="flex flex-wrap gap-4 mb-12">
          <button class="pixel-button flex items-center gap-2" style="padding: 12px 20px;">
            <span class="w-2 h-2 bg-green-400 rounded-full blink"></span>
            OPEN FOR WORK
          </button>
          <button
            class="pixel-button flex items-center gap-2"
            style="padding: 12px 20px; background: #2563eb; border-color: #1e40af;"
          >
            📧 EMAIL
          </button>
          <button
            class="pixel-button flex items-center gap-2"
            style="padding: 12px 20px; background: #14b8a6; border-color: #0f766e;"
          >
            🎮 PLAY
          </button>
        </div>

        <!-- Dotted line - Pixel style -->
        <div class="border-t-2 border-dashed border-cyan-400/50 my-8"></div>

        <!-- Footer info - Terminal style -->
        <div class="flex items-center gap-8 text-sm mono-font text-cyan-400 font-bold">
          <div class="flex items-center gap-2">
            <span class="w-3 h-3 bg-cyan-400 inline-block"></span>
            <span>portfolio_v2.0</span>
          </div>
          <div class="flex items-center gap-2">
            <span class="w-3 h-3 bg-blue-400 inline-block"></span>
            <span>2026</span>
          </div>
          <div class="flex items-center gap-2">
            <span class="w-3 h-3 bg-purple-400 inline-block"></span>
            <span>SETTINGS</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
